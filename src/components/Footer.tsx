const Footer = () => {
  return (
    <footer className="text-center p-4 text-sm text-gray-500">
      © {new Date().getFullYear()} Alvo Conquistado. Todos os direitos reservados.
    </footer>
  );
};

export default Footer;
